---
- hosts: all
  gather_facts: yes

- name: Generate /etc/hosts
  hosts: cluster
  become: true

  tasks:
    - name: Generate /etc/hosts file
      template:
        src: hosts.j2
        dest: /etc/hosts
